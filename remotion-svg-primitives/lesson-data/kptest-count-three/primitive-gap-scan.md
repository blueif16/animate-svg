# kptest-count-three — primitive gap-scan (W3b)

**Result: ZERO-GAP.** Every teaching-move `requires` (TEACHING-ACTIONS.md) is satisfied by a live catalog component (`src/capabilities/catalog-digest.md` is the only inventory source, consulted not primitive source). Default = compose existing primitives; **no new primitive ships, no registry build, no test stills.**

## REUSE table (teaching demand → catalog primitive; each row cites a live catalog component)

| cue / move | move `requires` | REUSE |
|---|---|---|
| topic-intro · announce-topic | title+teaser reads alone; cast enters after; `component`=LessonIntroCard | `lesson-intro-card` (`LessonIntroCard`) — eyebrow+title+teaser, write-on underline; apples enter next cue, never overlaid on the title |
| count-climb · count-on | one new item per spoken number, in sync; countables + ONE count badge (NOT badge AND highlight for one signal) | `countable-object` (`CountableObject`) variant=fruit ×3 (reward-orange teaching unit, identity-invariant); per-apple tag: `number-card` (`NumberCard`) digit 1/2/3, persistent; apple entrance: `pop-in` (`PopIn`) motion="snap" (entrance, not a count signal) |
| cardinality · reveal | focal change carries the discovery (the 3 re-scopes item-tag → whole-group number); narration does not pre-say | `cardinal-consolidation` (`CardinalConsolidation`) — N converging coral guide lines + per-item tags recede + one cardinal-total glyph settle-pops above; glyph accent: `pop-in` (`PopIn`) motion="bouncy" (the ONE climax accent) |

## Count-tag primitive — W3b owns (storyboard: "W3b picks the badge, confirm it persists")

**Chosen: `number-card` (`NumberCard`)** — a persistent per-apple digit card (STAYS once attached), one addressable instance per apple → its "3" can travel/migrate in cardinality (identity-preserved, no cross-fade), and it matches CardinalConsolidation's own per-item "cards." Its card surface IS the legibility-pill visual-design allowed ("no pill unless legibility needs" — a bare navy numeral over a row of orange apples needs the card).
**Rejected: `count-step-indicator` (`CountStepIndicator`)** — a transient step-punctuator ("pops and fades in to punctuate ONE step of the walk"); does NOT persist as a per-apple tag; its pop-and-fade would double-signal beside the apple PopIn; its avoid-list steers running totals elsewhere.
count-on single-signal (kids-eye §2): the NumberCard is the SOLE count mark per apple → NO highlight ring; sunshine not used anywhere.

## Non-catalog helpers / techniques (NOT registry components — not dual-form rows)

- `fitUnitsToZone` (auto-size-to-zone) — sizes + places the 3 apples in zone-objects; scene & manifest call identically.
- `EASE.*` / `SPRING.*` (motion-vocabulary) — named curves for entrance / tag-attach / converge / settle-pop.
- `breathe` (`Breathe`) — moving-hold on each cue's load-bearing group (apple row / card / glyph+quiet-apples), distinct phaseSeeds, amplitudeScale 0.005 (rule #6).
- Coral converging guide lines are CardinalConsolidation's own primitive graphic, NOT a sketch overlay → sketch / focus-pointer budget = 0; no `teacher-mark` (`TeacherMark`) gap named.

## Identity-invariant — composition, NOT a catalog gap

W3b confirms `cardinal-consolidation` (`CardinalConsolidation`) satisfies reveal's `requires` (the per-item-tag consolidate + recede + one total settle-pop IS the focal change). visual-design's "same 3 travels, no cross-fade" nicety is a W4 wiring choice — CardinalConsolidation's self-render, OR the count-climb `number-card` (`NumberCard`) "3" instance interpolated up+rescaled with converging lines — both resolvable within the catalog. The persistent one-per-apple NumberCard instances are set up exactly to keep the migration option open. reveal's `requires` is met either way → NOT a W3b catalog gap; no new primitive.
