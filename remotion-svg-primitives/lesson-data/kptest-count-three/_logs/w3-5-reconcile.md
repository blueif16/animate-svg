# W3.5 — Cue-timeline reconcile (kptest-count-three)

## INPUTS READ
- `lesson-data/kptest-count-three/visual-design.md` — per-cue **visualMotionSeconds**: topic-intro 3.5, count-climb 13.5, cardinality 6.5 (s). No W2b beat folded into a typed gap on a carrying cue → every clip id gets a budget entry.
- `src/lessons/generated/kptestCountThreeClips.ts` — FROZEN audio truth: 3 `ClipCue`s with `narrationFrames` 102 / 368 / 223, `clipSrc` 00/01/02-*.wav, `tokenOnsets` present, `caption`/`phrase` present, and **NO `gap`/`gapSeconds` field** on any cue → all `gapFrames = 0`. (`kptestCountThreeTotalNarrationFrames = 693`.)
- `lesson-data/kptest-count-three/brief.md` — length HINT ~25s (not a pad target); audience 3–5 Mandarin; builds-on none.
- `lesson-data/kptest-count-three/pedagogy.md` — 2-cue insight design: count-climb tri-repetition IS the age-appropriate drill for the half-known 一/二/三 sequence; cardinality = single delivery = landing/no-recap. (§8 exposure floor used below.)
- `lesson-data/kptest-count-three/storyboard.md` — `exposures` ledger present: 一=1, 二=1, 三=2. 3 cues topic-intro / count-climb / cardinality.
- `src/lessons/_cues/cueAccessors.ts` — `makeCueAccessors(cues, validIds?)` THROWS naming the unknown id (+valid ids); reads `AlignedLessonCue` + `cueMap` from the kit.
- `scripts/lesson-animatic.mjs` — gate flow: `loadReconciledCues` (tsx extractor) → **always** `bundle({entryPoint: src/index.ts})` → probe composition → scene-path OR timeline-only fallback (which STILL calls `bundle()` first).
- `scripts/_padded-cues-extract.ts` — reads `<camel>Cues`/`<camel>Duration`/`<camel>VoiceClips`; v4 path requires `narrationStartFrame`+`narrationEndFrame` on every cue; **silent `catch {}` → raw fallback** on import failure.
- `package.json` / `pipeline.json` — `lesson:animatic` = `scripts/lesson-animatic.mjs`; config `composition: CompleteKptestCountThreeLesson`, `voice.out: public/audio/kptest-count-three-voice.wav`.
- `src/lessons/kptestCountThreeLessonTimeline.ts` — STALE 03:24 (5 OLD cue ids intro/count-onset/cardinality-reveal/total-ask/total-recall, predating the revised 3-cue W2a/W3a). Overwritten (see below).
- Audio assets confirmed present: `public/audio/kptest-count-three-voice.wav` (1.13 MB), `public/audio/kptest-count-three/clips/{00-topic-intro,01-count-climb,02-cardinality}.wav`; ffmpeg+ffprobe on PATH.

## OUTPUTS WRITTEN
- `src/lessons/kptestCountThreeLessonTimeline.ts` — rewritten to the 3-cue design per the prescribed template: `VISUAL_MOTION_SECONDS as const { "topic-intro": 3.5, "count-climb": 13.5, cardinality: 6.5 }`; export `KptestCountThreeCueId` (union), `KPTEST_COUNT_THREE_CUE_IDS`; frozen-id import-time THROW (both directions — this module throws on a budget key not in `kptestCountThreeClips`; `reconcileClipTimeline` throws on an unbudgeted clip); `reconcileClipTimeline({ clips, visualBudgets: VISUAL_MOTION_SECONDS, fps: 30, tailFrames: 9 })`; exports `kptestCountThreeCues`, `kptestCountThreeDuration`, `kptestCountThreeVoiceClips` (`VoiceClip[]`), `kptestCountThreeLessonCaptionCues` (`cueToCaption`), `kptestCountThreeCueAccessors` (`makeCueAccessors(reconciled.cues, KPTEST_COUNT_THREE_CUE_IDS)`). NO `PADDED_CUE_DURATIONS_FRAMES`, NO continuous-WAV span, NO `detectSilence`/`Silences`/`ASR_CORRECTIONS`, NO `c[id]?.startFrame ?? 0` fallback, NO frame/raw-motion literals.
- `lesson-data/kptest-count-three/_logs/w3-5-reconcile.md` — this log.

## COMMANDS RUN (exit + key output)
- `npx eslint src/lessons/kptestCountThreeLessonTimeline.ts` → **exit 0** (clean; 0 errors. ESM imports only; no unused imports/vars; no React components.)
- `npm run lesson:animatic -- --config lesson-data/kptest-count-three/pipeline.json` → **exit 1**. FAILED at webpack `bundle()` (runWebpack): enhanced-resolve cannot resolve `@studio/narration-kit` ("Field 'browser' doesn't contain a valid alias configuration … index.js doesn't exist", then walks up `/Users/tk/Desktop/node_modules`, `/Users/node_modules`, `/node_modules`). Root cause = the kit symlink `node_modules/@studio/narration-kit -> ../../../../shared-narration` realpath = `/Users/tk/Desktop/shared-narration`, OUTSIDE this node's read-scope. NOT a timeline defect.
- `node_modules/.bin/tsx scripts/_padded-cues-extract.ts kptestCountThree` → **exit 0** BUT `source:"raw"`, `voiceClips:[]`, cues frames `8/122/496` — i.e. my timeline module's `import()` THREW ("Cannot find module '@studio/narration-kit'"), was silently caught, and the extractor fell back to `generated/kptestCountThreeTiming.ts`. The gate therefore rendered the WRONG (raw) cues, not my reconciled ones.
- `node_modules/.bin/tsx -e "import('./src/lessons/kptestCountThreeLessonTimeline.ts')"` → **IMPORT ERR: "Cannot find module '@studio/narration-kit'"** (Require stack: my timeline.ts).
- `node -e readFileSync`/`ls -L` on `/Users/tk/Desktop/shared-narration` → **EPERM "operation not permitted"** (kit realpath outside read-scope). `test -d`/`realpath` of the symlink succeed (existence/path only), but content reads are denied.

## KEY DECISIONS
- **Budget set = clip set, 1:1.** 3 VISUAL_MOTION_SECONDS entries match exactly the 3 frozen clip ids; verified NO `gap` field on any ClipCue → `gapFrames = 0` everywhere (FREE silence is just each cue's `tail` + the narration<visual slack, owned by the composer as a motion hold).
- **Followed the prescribed module template verbatim** (imports, `as const` budget, Pascal/UPPER_SNAKE cue-id union, frozen-id throw, `reconcileClipTimeline`, exports, `makeCueAccessors`). The reconcile is computed BY THE KIT at module load — I hardcode no frames; `VISUAL_MOTION_SECONDS` carries the W2a *seconds* (intent).
- **Hand-replicated reconcile** (kit spec: `cueFrames = max(narrationFrames + gapFrames, round(visualMotionSeconds*fps)) + tailFrames`; FPS=30, TAIL=9, gap=0; chain end-to-end) — for VERIFICATION ONLY, nothing baked into the module:

  | cue | narrFr | round(motion·30) | max | +tail | window | clip [from,dur]→end | fits? |
  |---|---|---|---|---|---|---|---|
  | topic-intro | 102 | 105 | 105 | 114 | 0–114 | 0 + 102 → 102 | ✓ 102≤114 |
  | count-climb | 368 | 405 | 405 | 414 | 114–528 | 114 + 368 → 482 | ✓ 482≤528 (45f motion-hold slack) |
  | cardinality | 223 | 195 | 223 | 232 | 528–760 | 528 + 223 → 751 | ✓ 751≤760 (9f tail) |

  **Total = 760 frames = 25.33s** (brief hint ~25s → drift accepted, never padded). **cardinality is narration-driven** (audio 7.43s > visual 6.5s): audio FROZEN — the cue is NOT extended; the composer fits the ~6.5s climax (converge 0.8 + migrate 0.6 + settle-pop 1.0 + holds ≈4.1) inside the 7.43s narration window + tail.
- **Gate verdict, hand-replicated** (condition `clipStart >= cue.startFrame && clipEnd <= cue.endFrame`; VoiceClip spec = clip at `cue.startFrame` for `narrationFrames`): all 3 clips **FIT** → the animatic's fit verdict **WOULD be PASS** (no overrun, no dropped cue, no degenerate zero-length).

## SELF-AUDIT / CHECKS
- ✓ DRIVER-CHECKS regex: `reconcileClipTimeline(` present; `makeCueAccessors(` present; absent `PADDED_CUE_DURATIONS_FRAMES`/`detectSilence`/`Silences`/`ASR_CORRECTIONS`; absent `?? -?\d` fallback.
- ✓ lint: `npx eslint <my file>` → 0 errors.
- ✓ frozen-id bidirectional: every budget key ∈ `kptestCountThreeClips` (import-time throw); `reconcileClipTimeline` throws on any unbudgeted clip; 3 = 3.
- ✓ determinism: module calls `reconcileClipTimeline` — no frame/raw-motion literals; no `useFrame`/`Date.now`/`Math.random`.
- ⚠ **animatic gate: NOT executable in this node's sandbox** (kit realpath outside read-scope → EPERM → bundle + timeline-import both fail). Verdict established by hand-replication = PASS (see above + ISSUES).

## ISSUES
- **[GATE-COULD-NOT-RUN-IN-SCOPE — environment, not artifact]** `lesson:animatic` could not complete in this node's bash sandbox: `@studio/narration-kit` (npm dep `"file:../../shared-narration"`).realpath = `/Users/tk/Desktop/shared-narration`, **outside this node's read-scope** (repo + `.agents` only). Reading its `package.json`/contents returns **EPERM** → (a) webpack/enhanced-resolve cannot resolve the package → `bundle()` throws (animatic exit 1); (b) tsx `import` of my timeline throws `"Cannot find module '@studio/narration-kit'"` → the extractor SILENTLY falls to the raw Timing module (`source:"raw"`). Consequently `reconcileClipTimeline` could NOT be empirically executed here. Soundness established instead via lint-clean + static regex checks + hand-replicated deterministic reconcile + hand-replicated gate clip-fit verdict = **PASS for all 3 cues**. The harness's animatic re-run will resolve the kit only in a scope that includes the shared-* realpath.
- **[COMPREHENSION-FLOOR WARN — advisory, NON-blocking]** storyboard `exposures` ledger: 一=1, 二=1, 三=2 — all UNDER pedagogy §8 floor (≥6–10 spaced exposures; ≥3–5s wait-time per echo). Flagged per the advisory (never blocking). Per pedagogy this is by-design: count-climb's tri-repetition (3 one-to-one arrivals) IS the age-appropriate drill for the half-known sequence; cardinality is a single-delivery insight/landing with no recap — so low single/double exposures are intended, not an oversight. Reported anyway as required.
- **[STALE W4 FILES — not W3.5's scope, flagged for W4]** the 03:24 run left 5-cue downstream files (`CompleteKptestCountThreeLesson.tsx`, `manifest.ts`, `kptestCountThreeLessonScene.tsx`, `_qaStillEntry.tsx`, `_lessonRegistry.generated.tsx`) referencing OLD cue ids (intro/count-onset/cardinality-reveal/total-ask/total-recall). They will THROW at runtime via `kptestCountThreeCueAccessors` (unknown-id) once the kit is readable and any scene-path render runs. W4 (composer) owns these and must rewrite to the new 3-cue design before the scene-path animatic can PASS. Not edited here (out of my declared owns).

## PIPELINE FINDINGS
- **W3.5 read-scope omits the shared-* realpath targets.** `@studio/narration-kit` (+`shared-sound`,`shared-3d`) are npm deps whose symlink realpath = `/Users/tk/Desktop/shared-*`, OUTSIDE W3.5's read-scope (repo + `.agents`); yet W3.5 is REQUIRED to run `lesson:animatic`, which `bundle()`s the full entry graph and the timeline-only fallback ALSO calls `bundle()` first. In any sandbox without the shared-* packages readable, the animatic gate is unsatisfiable BY DESIGN. FIX: (A) add the `/Users/tk/Desktop/shared-*` realpath targets to W3.5's read-scope; and/or (B) `scripts/lesson-animatic.mjs` should reach the timeline-only fallback WITHOUT bundling — it already loads cues via the tsx extractor BEFORE `bundle()`; move the scene-probe/fallback decision before `bundle()` and/or wrap `bundle()` in try/catch so a bundle failure still yields the timeline-only verdict.
- **`_padded-cues-extract.ts` silently swallows the reconciled-timeline import** (`catch {}` → raw fallback), masking a kit-resolution failure OR a genuine timeline bug as `source:"raw"` and rendering the WRONG (raw) cues. Suggest a stderr WARN (`reconciled timeline import failed; falling back to raw: <err>`) so the substitution is visible (here it hid that the timeline wasn't being used at all).
- **(Nice-to-have)** exposing `reconcileClipTimeline` as a pure TS function importable without the full TTS/ffmpeg runtime would let W3.5 verify the reconcile deterministically even when only the core kit is resolvable.
