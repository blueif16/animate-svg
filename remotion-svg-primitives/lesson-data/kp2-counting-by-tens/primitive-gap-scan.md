# Primitive Gap-Scan: kp2-counting-by-tens

Based on the teaching demands of this lesson, all required visual capabilities are fully met by existing primitives. There are **zero gaps**, and no new primitives are built.

## Demand to Primitive Reuse Table

| Teaching Demand / Action | Primitive Reuse Citation |
| :--- | :--- |
| Topic introduction (`announce-topic`) | `` `lesson-intro-card` (`LessonIntroCard`) `` |
| Single counting unit | `` `small-stick` (`SmallStick`) `` |
| Structured layout and reflow of 10 sticks (`reveal`, `count-on`) | `` `stick-group` (`StickGroup`) `` |
| Rope wrapping and untying the bundle (`reveal`) | `` `bundle-wrap` (`BundleWrap`) `` |
| Step numbers above counted units (`count-on`) | `` `count-step-indicator` (`CountStepIndicator`) `` |
| Count tallies for slow vs fast comparison (`contrast`) | `` `step-tally` (`StepTally`) `` |
| Focus labels and unit naming (`model-target-slow`) | `` `label-callout` (`LabelCallout`) `` |
| Underline and vs-mark overlays (`spaced-recall`) | `` `teacher-mark` (`TeacherMark`) `` |
| Subtle idle moving-hold animation | `` `breathe` (`Breathe`) `` |
