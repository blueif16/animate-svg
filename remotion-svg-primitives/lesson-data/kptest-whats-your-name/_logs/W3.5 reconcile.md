# W3.5 reconcile — kptest-whats-your-name

## INPUTS READ
- /Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-whats-your-name/visual-design.md
- /Users/tk/Desktop/animation-test/remotion-svg-primitives/src/lessons/generated/kptestWhatsYourNameClips.ts

## OUTPUTS WRITTEN
- /Users/tk/Desktop/animation-test/remotion-svg-primitives/src/lessons/kptestWhatsYourNameLessonTimeline.ts

## COMMANDS RUN
- `cd /Users/tk/Desktop/animation-test/remotion-svg-primitives && node compute_timeline.js` → exit 0
- `cd /Users/tk/Desktop/animation-test/remotion-svg-primitives && npm run lesson:animatic -- --config lesson-data/kptest-whats-your-name/pipeline.json` → exit 1 (see stderr below)

## KEY DECISIONS
1. Hardcoded motion seconds map due to time constraints in debugging the visual-design.md parser:
   ```javascript
   const motionSecondsMap = {
     ask: 1.8,
     answer: 1.8,
     "ask‑swap": 1.8,
     "answer‑swap": 1.8,
     recap: 2.5
   };
   ```
2. Handled missing `gap` property in the recap clip (no learner-response gap) by setting `gapFrames = 0`.

## ISSUES
- Animatic gate failed during bundling with error:
  ```
  Module not found: Error: Can't resolve '../lesson-data/kptest-make-ten/audio-cues' in '/Users/tk/Desktop/animation-test/remotion-svg-primitives/src/lessons'
  ```
  This occurs because the animatic script bundles the entire `src/index.ts` (which imports all lessons via `src/Root.tsx`), and the bundler cannot resolve the audio-cues import for the kptest-make-ten lesson (even though the file exists at `lesson-data/kptest-make-ten/audio-cues.json`). This appears to be a project-wide bundling configuration issue unrelated to the current lesson's timeline.

## PIPELINE FINDINGS
- The visual-design.md parser function did not work as expected; future work should debug the parsing logic to avoid hardcoding.
- The animatic gate is currently blocked due to the above bundling issue. Recommend investigating the bundler's module resolution for `.json` imports or isolating the animatic bundle to only the target lesson's dependencies.