# W1 Storyboard Log

## INPUTS READ
- /Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-classroom-objects/brief.md
- /Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-classroom-objects/pedagogy.md
- /Users/tk/Desktop/animation-test/.agents/TEACHING-ACTIONS.md

## OUTPUTS WRITTEN
- /Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-classroom-objects/storyboard.md

## COMMANDS RUN
No external commands were run; only file reads and write.

## KEY DECISIONS
- Defined 11 cues: three for pencil (reveal, invite-echo, replay), three for pen (reveal, invite-echo, replay), four for ruler (reveal, first invite-echo, additional invite-echo, replay), and one for recall-together.
- Used teaching actions: model-target-slow, invite-echo, replay, spaced-recall.
- Narration intent focused on the English target word being spoken slowly and clearly, with Chinese framing where appropriate.
- Required visual derived from each teaching action's `requires` field.
- Exposures ledger set to { pencil: 3, pen: 3, ruler: 4 } based on counting model exposures in acquisition cues.

## ISSUES
- Ambiguity in `invite-echo` teaching action: audio requirement specifies "model line + held silent beat" but does not include the invitation phrase ("跟我说：..."). However, the brief states teacher narration is in Chinese, implying the invitation should be spoken. This may require revisiting the action design or adjusting expectations.
- For ruler's additional echo choral cue, we used a second `invite-echo` cue, which includes a model line. The pedagogy describes this as an echo choral cue after the wait-time gap, which might be better served by a distinct action (e.g., echo-only) to avoid redundant modeling.
- The `recall-together` cue aggregates pointing and waiting for three objects into a single cue. While permissible, it may complicate precise alignment of narration and visual highlights; consider breaking into sub-cues if timing issues arise in later waves.
- The `model-target-slow` visual requirement calls for the target glyph "big, centered, nothing on top," but our required visual includes both the object icon and the English word. Need to clarify whether the word alone should be prominent or if the object-plus-word layout satisfies the requirement.

## PIPELINE FINDINGS
- Consider proposing a new teaching action `echo-only` (or similar) for pure invitation/response beats without remodelling the target, to better match pedagogical patterns like the additional echo for ruler.
- Verify with the visual-design wave whether the object-plus-word layout for `model-target-slow` meets the "target glyph big, centered, nothing on top" requirement, or if the word should be isolated.
- Ensure the exposures ledger aligns with the intended acquisition dosage; current counts may need adjustment after clarifying cue definitions.