composition:             1920×1080 @ 30fps   (fixed; non-negotiable)
short-side:              1080 px

teaching-unit:           single 小棒 (one loose stick)
teaching-unit-min:       ≥ 8% of short-side = 86 px   →  90 px
teaching-unit-target:    12–15% of short-side = 130–160 px   →  140 px

primary-label-min:       48 px rendered  (signal labels: "一个十", takeaway lines)
body-label-min:          36 px rendered  (counts, badges)
caption-line-min:        56 px rendered
chrome-label:            FORBIDDEN — if a label is decoration, delete it

zone-objects:            holds the bundle of ten sticks (fixed on left) and the loose sticks that appear on the right (as they drop in). NOTHING ELSE.
zone-labels:             holds the label "一个十" for the bundle and the takeaway phrase "十个一=一个十". Never inside zone-objects.
zone-badges:             holds count step indicators (small circular badges) above each loose stick as it appears, indicating the current count.
zone-tally:              holds a step-tally showing the running count of loose sticks (as dots or numeric).
zone-caption:            holds the caption ribbon at the bottom for narration text.
zone-marks:              full-bleed for sketch marks (teacher's ink marks). Marks may TRACE OVER zone-objects (e.g. wrap-arc following a tie path) but never SIT INSIDE zone-labels.

metaphor:        A bundle of ten tied sticks on the left equals "一个十"; each loose stick added to the right adds "一个一", forming the teen number word as the total.
regions:         zone-objects holds sticks (bundle fixed left, loose sticks accumulating right); zone-labels holds "一个十" and takeaway "十个一=一个十"; zone-badges holds per-stick count indicators; zone-tally shows running count of loose sticks; zone-caption holds narration ribbon.
between-states:  the bundle of ten sticks persists unchanged on the left; the number of loose sticks on the right increases by one for each new teen number (from 1 to 9); the same stick primitives are reused throughout, only their layout changes (fixed bundle vs. accumulating loose sticks).
reading-order:   eye lands on bundle (left), then loose sticks (right), then number word in caption or label, then takeaway if present.
decoration-budget:  max 2 stacked surfaces (e.g., cream background + caption ribbon); max 4 meaningful colors (reward orange for sticks, coral for accents, textNavy for text/sketch, sunshine for transient highlight).
text-budget:     "一个十" (load-bearing — names the bundle unit), takeaway "十个一=一个十" (load-bearing — the moral), number words like "十一" in caption (load-bearing — the teen number being taught), captions (load-bearing — accessibility). No chrome labels.
occupancy:       horizontal axis is binding (loose sticks added horizontally to the right of the fixed bundle); teaching unit (one stick) at 140 px = 13% of 1080 short-side, hits target per kids-eye. Bundle width ~200 px (10 sticks) occupies significant horizontal space.
identity-invariant: every stick is a SmallStick at the same reward tone (#FFB84D) for the entire video, whether in the bundle or loose, ensuring visual continuity of the teaching unit.
motion-budget:   announce-topic: 2.0s (title write-on + settle); model-target-slow: 3.0s (static display dwell for slow modeling and encoding); invite-echo: 3.0s (static display dwell during model and child-response gap).