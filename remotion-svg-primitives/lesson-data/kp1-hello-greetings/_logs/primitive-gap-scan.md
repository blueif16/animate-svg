# W3b — primitive gap-scan/build — kp1-hello-greetings

## INPUTS READ
- lesson-data/kp1-hello-greetings/visual-design.md (Visual Contract, zones, per-cue choreography)
- lesson-data/kp1-hello-greetings/pedagogy.md (the 5 discoveries the build must serve)
- lesson-data/kp1-hello-greetings/brief.md (names DialogueExchange + ReadAlongHighlight; two identity-invariant kids)
- src/capabilities/catalog-digest.md (authoritative reuse menu — confirmed lesson-intro-card, dialogue-exchange, read-along-highlight, pulse-circle, popin, breathe, signal-focus-pointer, icon-asset all exist)
- .agents/CAPABILITIES.md (motion/sketch/asset protocol; generated-decorative-SVG fence)
- src/shape-primitives/index.ts (barrel exports)
- src/motion-primitives/DialogueExchange.tsx (figure contract: left/right.figure = caller ReactNode)
- src/motion-primitives/ReadAlongHighlight.tsx (item render: items wrapped in <g color=tint>; item must be a <text>, not a bare <tspan>)
- src/scenes/ReadAlongHighlightDemo.tsx + DialogueExchangeDemo.tsx (correct item/figure form)
- src/shape-primitives/literacy.tsx (LessonIntroCard prop surface — confirmed it fits the English-greetings title)
- public/icons/boy-face.{svg,meta.json} (the one existing kid face)
- src/Root.tsx + src/Composition.tsx + scripts/lesson-primitive-checks.mjs (still-render wiring)
- /Users/tk/Desktop/Omniscience/.env (GOOGLE_API_KEY present, 39 chars) + scripts/gen_asset_svg.py

## OUTPUTS WRITTEN
- lesson-data/kp1-hello-greetings/primitive-gap-scan.md (REWRITTEN — superseded a stale scaffolded draft)
- public/icons/girl-face.svg + public/icons/mono/girl-face.svg + public/icons/girl-face.meta.json (NEW generated asset)
- src/shape-primitives/iconAssetData.ts (regenerated by icons:build — 90 assets)
- src/lessons/kp1HelloGreetings/primitiveChecks.tsx (NEW — two W3b verification scenes)
- src/Composition.tsx (export the two check scenes + duration)
- src/Root.tsx (import + register the two PrimitiveCheckKp1HelloGreetings* compositions)
- out/kp1-hello-greetings/primitive-checks/Hardest.png + Multiplicity.png (verified test stills)
- out/girl-face.{raw,keyed}.png (generation intermediates)

## COMMANDS RUN (+exit + key output)
- gen_asset_svg.py --name girl-face (1st, "warm orange shirt") — exit 0; traced skin→coral (REJECTED: collides with reserved coral channel)
- gen_asset_svg.py --name girl-face (2nd, "peach skin + sky-blue shirt + brown pigtails") — exit 0; layers {yellow,blue,skin,slate,stroke}; NO coral. ACCEPTED.
- npm run icons:build — exit 0; "wrote 2 file(s) (90 asset(s))" incl. girl-face
- npm run registry:build — exit 0; in sync (38 primitives, 18 motion, 6 fx, 62/62 documented)
- npx tsc --noEmit — exit 0; 0 errors (new scene + wiring typecheck)
- node scripts/lesson-primitive-checks.mjs --config lesson-data/kp1-hello-greetings/pipeline.json — exit 0; 2 stills written (re-run 3× while fixing read-along item form + farewell gap)
- npm run registry:check — exit 0; icons 90 in sync / catalog in sync / digest in sync / schema in sync / all 62 components have a gallery demo
- npm run lint (eslint src && tsc) — exit 0; clean (no output)

## KEY DECISIONS
- ZERO new hand-coded primitives. The brief-named DialogueExchange + ReadAlongHighlight + the existing LessonIntroCard cover the entire spoken-routine spine + the mandated intro card. visual-design's "GAP-1 topic-intro card" is REUSE (lesson-intro-card), not a gap.
- The ONE real gap = a SECOND identity-invariant kid face (only boy-face existed; robot/cloud faces are not kids). Built as a GENERATED decorative ASSET (girl-face), NOT a hand-coded primitive — all three generation fences hold (fixed-form, decorative cast, recognizability-is-the-bug). DialogueExchange takes the face as a caller ReactNode, so an asset is the correct home.
- Regenerated girl-face after the first pass posterized skin to the lesson's reserved coral channel — steered to peach skin + sky-blue shirt so the asset fills never collide with the coral emphasis meaning.
- Stills rendered at the REPO's real composition size (1280×720, theme video), not visual-design's stated 1920×1080 — no lesson renders at 1920 (flagged as a pipeline finding).
- Fixed read-along item form in the check scene: full <text fill="currentColor"> per word, not bare <tspan> (which renders invisible).

## ISSUES
- visual-design.md states 1920×1080 but the actual fixed composition is 1280×720 — the composer must build at 1280×720.
- The scaffolded primitive-gap-scan.md carried stale verdicts (intro-card as a gap; girl-face BLOCKED on GOOGLE_API_KEY) — both false now; rewrote the file.

## PIPELINE FINDINGS
- visual-design §1 measurement block should state the ACTUAL 1280×720 render size (or bump theme `video` to 1920 if that is the intent).
- ReadAlongHighlight JSDoc example (`<tspan fill="currentColor">`) is wrong/misleading — items must be `<text>` nodes; the bare tspan renders invisible. Fix prose + JSDoc.
- No `lesson:primitive-checks` npm script wires scripts/lesson-primitive-checks.mjs — add one so the wave doesn't invoke node directly by hand.
- The gap-scan scaffold should seed an EMPTY table, not pre-filled verdicts, so a wave isn't forced to detect-and-reverse prior guesses.
- character-face asset coverage is still thin (now boy + girl). One generation burst could add a small cast set now that the key is wired, so future multi-character lessons aren't one asset short each time.
