# Setup / Scaffold — kptest-compare-more-fewer

## INPUTS READ
- `.agents/skills/complete-video-pipeline/SKILL.md` (folder/command contract)
- `lesson-data/kptest-compare-more-fewer/brief.md` (pre-existing; verified verbatim match to provided BRIEF TEXT — left untouched)
- `lesson-data/kptest-compare-more-fewer/pipeline.json` (generated by scaffold, read to extract declared paths)

## OUTPUTS WRITTEN
- `lesson-data/kptest-compare-more-fewer/pipeline.json` (scaffolded — mechanical, not hand-edited)
- `lesson-data/kptest-compare-more-fewer/_logs/setup-scaffold.md` (this log)
- brief.md NOT written — it already existed and matched the BRIEF TEXT byte-for-byte.

## COMMANDS RUN
- `npm run lesson:scaffold -- --id kptest-compare-more-fewer` → exit 0
  - stdout: `Wrote lesson-data/kptest-compare-more-fewer/pipeline.json`
  - `lessonId: kptest-compare-more-fewer`
  - `composition: CompleteKptestCompareMoreFewerLesson`
  - `constPrefix: kptestCompareMoreFewer`
- `wc -c` on both artifacts → brief.md 1706 bytes, pipeline.json 676 bytes (both non-empty)

## KEY DECISIONS
- brief.md present + identical → left untouched per STEP 1.
- pipeline.json missing → scaffolded via the shared script (not hand-edited per OWNED-PATH NOTE).
- Created `_logs/` dir for this tier-2 log.

## DECLARED PATHS (pipeline.json — source of truth for later waves)
- lessonId: `kptest-compare-more-fewer`
- Style (from brief): `default`
- composition name: `CompleteKptestCompareMoreFewerLesson`
- constPrefix: `kptestCompareMoreFewer`
- timing module (ASR/voice align, `voice.alignOutTs`): `src/lessons/generated/kptestCompareMoreFewerTiming.ts`
- cuePlan: `lesson-data/kptest-compare-more-fewer/script-cues.json`
- voice wav: `public/audio/kptest-compare-more-fewer-voice.wav`
- render output: `out/kptest-compare-more-fewer/kptest-compare-more-fewer.mp4`
- gemini-voice meta: `out/kptest-compare-more-fewer/gemini-voice.json`
- asr-alignment: `out/kptest-compare-more-fewer/asr-alignment.json`
- **Timeline module (W3.5 reconcile target, derived by convention from constPrefix — NOT a pipeline.json field):** `src/lessons/kptestCompareMoreFewerLessonTimeline.ts` (per CLAUDE.md: `src/lessons/<X>LessonTimeline.ts`; per-cue clip module `src/lessons/generated/kptestCompareMoreFewerClips.ts`).

## ISSUES
- None blocking. pipeline.json declares the timing module, cuePlan, voice, and output paths explicitly, but does NOT declare the `<X>LessonTimeline.ts` timeline-module path nor the per-cue `<X>Clips.ts` path — those are derived by convention from `constPrefix` (`kptestCompareMoreFewer`). Recorded as a pipeline finding.

## PIPELINE FINDINGS
- The scaffolded pipeline.json declares only `alignOutTs` (the ASR timing module). The W3.5 reconcile timeline path (`src/lessons/<X>LessonTimeline.ts`) and the v4 per-cue clip module (`src/lessons/generated/<X>Clips.ts`) are NOT declared in pipeline.json and must be derived from `constPrefix` by each later wave. Making the scaffold emit these two paths explicitly (e.g. `timelineModule`, `clipsModule`) would remove a derive-by-convention step and a drift risk for W3.5/W4.
