# W4b — Sketch Overlay (kptest-fenyuhe-six)

## INPUTS READ

- `/Users/tk/Desktop/animation-test/.agents/skills/sketch-explainer-layer/SKILL.md` — read in full; followed LITERALLY (cue-relative framing rule, restraint principle, mark vocabulary authorization, anchor discipline, output structure §1–§4, self-check).
- `/Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/pedagogy.md` — 9-cue discovery list (the 3 echo beats `echo-1-and-5` / `echo-2-and-4` / `echo-3-and-3` are folded into the 3 split cues' typed gaps in the v4 reconcile). The lesson's "one beat" is the equal split 3和3; the highlight is expressed as extra symmetric DWELL (not extra cues), per §8 "breadth before depth."
- `/Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/visual-design.md` — Visual Contract §4 (single-metaphor six dots, bond glyph, identity invariant), palette §3 (`textNavy` as the single ink; cream + reward + textNavy = 3 meaningful colors), motion vocabulary §3 (EASE.outQuint is the ONE climax curve, on split-3-and-3), zones §2 (zone-title / zone-objects / zone-pointer / zone-caption, all disjoint), **§3 "Sketch tone: N/A — this lesson carries no teacher marks"** (the load-bearing upstream directive), §6 reuse table (no `<TeacherMark>` listed for this lesson), anti-patterns §7.
- `/Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/storyboard.md` — per-cue teaching actions + required visuals; the 9-cue storyboard compresses to 6 v4 cues (3 echoes folded into 3 split cues' gaps). The storyboard requires `<PointerHandArrow variant="hand">` or `<PulseCircle>` as the "your turn" affordance — explicitly characterized as "signaling chrome, not a sketch mark."
- `/Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/brief.md` — KP (6的分与合), audience (一年级, Mandarin), builds-on (kptest-fenyuhe-five), highlight (3和3 is the new property).
- `/Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/audio-captions.md` — 6 v4 audio cues (the v4 audio-cue compression) + per-cue narration text. Verified: no audio cue's narration asks the picture for ink reinforcement.
- `/Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/audio-cues.json` — sound manifest (bed + SFX); per-cue SFX = `ta-da` on `split-3-and-3`, `chime` on `recap`. SFX is audio, not ink.
- `/Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/script-cues.json` — 6 v4 cue ids match the W3.5 reconcile output.
- `/Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/_logs/w3-5-reconcile.md` — the v4 cue-anchored reconcile; per-cue boundaries + length are the AUDIO TRUTH for the v4 run. The "key stdout-stderr" block lists 6 cues / 1675 fr total.
- `/Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/_logs/w3b-primitive-build.md` — REUSE list (8 registered primitives; `<TeacherMark>` is in the list because it's in the catalog, but the W3b scan did not name a specific beat for it; the visual-design's "N/A" is the upstream call that wins).
- `/Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/_logs/w4a-composer.md` — the prior-run W4a composer state (uses pre-rename 9 cue ids; has an `underlineStateAt` helper for a `cue-split-3of3` underline that the visual-design does not authorize). This is a drift; flagged in pipelineFindings.
- `/Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/_logs/w4b-sketch.md` (this file's predecessor) — the prior-run W4b sketch spec, which used pre-rename cue ids and a 1-mark `cue-split-3of3` underline. Superseded by this run.
- (Did NOT read primitive source, did NOT consult any other lesson's sketch-overlay or any other lesson's artifacts — the SKILL + catalog + this lesson's own upstream are the COMPLETE set per the READING LAW.)

## OUTPUTS WRITTEN

- `/Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/sketch-overlay.md` — the spec consumed by the W4 composer. 0 marks across 6 v4 cues; per-cue table maps every cue to its already-saturated signal.
- This log file.

## COMMANDS RUN

- `ls -la /Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/` — confirmed all upstream artifacts (brief, storyboard, pedagogy, visual-design, audio-captions, audio-cues, pipeline, script-cues, _logs) are present.
- `ls /Users/tk/Desktop/animation-test/remotion-svg-primitives/lesson-data/kptest-fenyuhe-six/_logs/` — confirmed W3-5-reconcile.md is the latest upstream (mtime 11:40, the v4 re-run; the prior W4a / W4b logs are from 10:12, pre-rename).
- `stat` on the prior W4a scene file — confirmed 32,938 bytes, mtime 10:13 (pre-W3.5-rename). W4a has NOT been re-run since W3.5's v4 cue-id rename; the prior W4a still uses the 9 cue-id namespace and still has the `underlineStateAt` helper. The new W4b spec (this run) is the upstream-validated call to remove that helper.
- (No npm runs; W4b is a spec-only node — no rendering, no audio, no asset build.)

+exit 0
+key stdout-stderr: (no stderr; stdout is the read-tool listing above)

## KEY DECISIONS

1. **Zero marks, per the upstream `visual-design.md` §3 directive.** The visual-design's "Sketch tone: N/A — this lesson carries no teacher marks" is the load-bearing upstream call. The pedagogy doesn't ask for marks; the storyboard doesn't ask for marks; the W3b gap-scan didn't name a beat for a mark (TeacherMark is in the registry's REUSE list because it's registered, but the W3b scan did not designate a use). The W4b spec honors the upstream call. The previous-run W4b spec's 1-mark on `cue-split-3of3` is a drift from the visual-design — it added ink the design forbids, and its cue id (`cue-split-3of3`) doesn't even exist in the v4 timeline. This run replaces it with a 0-mark spec.

2. **v4 cue ids throughout.** The 6 cue ids from the W3.5 reconcile (`routine-reprise`, `split-1-and-5`, `split-2-and-4`, `split-3-and-3`, `aggregator-prompt`, `recap`) are the v4 namespace. The prior W4b spec's 9 cue ids (`cue-announce-split-1of5`, `cue-conserve-1of5`, `cue-split-2of4`, `cue-conserve-2of4`, `cue-split-3of3`, `cue-conserve-3of3`, `cue-learner-response-gap`, `cue-reveal-answer`, `cue-spaced-recap-all-three`) are the pre-rename namespace and do not exist in the v4 timeline. The 3 echo beats from the storyboard (the 9-cue structure) are folded into the 3 split cues' typed gaps in v4 (per W2b's v4 audio-captions + the v4 brief: "the gap carries the learner-response hold"). The new W4b spec uses the 6 v4 ids and refers to the gap-bearing split cues by their v4 names.

3. **The equal-split climax is celebrated by the picture, not by ink.** Per pedagogy §"The one beat" + §cue-split-3of3: *"6 splits exactly in half — 3 and 3 — and 3 and 3 combine back into 6; the equal split is a new property of 6 felt as symmetry."* The pedagogy says the equality is FELT (the picture's job) and explicitly forbids the voice from pre-announcing "they are equal" (§4 leakage rule). The visual-design's motion vocabulary reserves the ONE `EASE.outQuint` climax curve for `split-3-and-3`, the symmetric hold gets extra DWELL seconds, the `<Breathe>` wrap gives the held state micro-motion, and the bond glyph + voice name the bond. A teacher underline on the symmetric hold would re-articulate a signal the picture has already delivered four times over (mirror placement + climax curve + symmetric hold + extra dwell). The §"Restraint principle" sentence cannot be completed for `split-3-and-3`. Drop the mark.

4. **All 6 cues complete the "signal X, which is not yet carried by Y or Z" check as "no mark needed."** Per the per-cue table §2:
   - `routine-reprise`: signal = "this is the same routine as 5"; carried by the title card (`<LessonIntroCard>`) + the six dots entering as a row + the voice naming "六的分与合。和五的分与合一样，我们来分六。". No gap.
   - `split-1-and-5`: signal = "6 can be split into 1 and 5"; carried by `<PartWholeComposer mode="split" then mode="merge">` on the same six dots + the bond glyph "一和五" + the voice naming "六可以分成一和五。一和五合成六。". No gap.
   - `split-2-and-4`: same shape as `split-1-and-5` (co-equal airtime per pedagogy §8). No gap.
   - `split-3-and-3`: signal = "6 splits exactly in half — symmetric"; carried by the 30f `EASE.outQuint` climax motion + the mirror placement + the 2s symmetric hold + the 5s extra-dwell gap + the bond glyph + the voice + the SFX `ta-da`. Five-layer delivery, no gap.
   - `aggregator-prompt`: signal = "retrieve the full set"; carried by the held six-dot row + the `<PulseCircle>` affordance + the voice's "六可以分成几和几？" + the 4s typed gap for retrieval. No gap.
   - `recap`: signal = "confirm the three splits in canonical order"; carried by `<RecapSpotlight currentHighlight={i}>` with the live highlight following the spoken item + the voice's "一和五。二和四。三和三。它们合起来，都是六。" + the SFX `chime`. No gap.

5. **The composer hand-off explicitly tells W4a to drop the prior `underlineStateAt` helper.** The prior W4a composer (pre-rename, mtime 10:13) built a `underlineStateAt` helper for a `cue-split-3of3` underline. The new W4b spec's §4 is the load-bearing hand-off: the W4a re-run (after the W3.5 cue-id rename) MUST remove the `underlineStateAt` helper, remove the `<TeacherMark>` import from the scene's import block, and verify that no `<TeacherMark>` element is mounted anywhere in the scene. The previous W4a spec was a 1-mark spec; the new W4b spec is a 0-mark spec; W4a is downstream of W4b and must follow.

6. **No new primitives, no new components.** This node is a spec — no source writes, no primitive builds, no registry rebuild. The W3b REUSE list (8 primitives, 0 GAPs) is unchanged. The W4b's role is to say "the 8 REUSE primitives are correct; no additional ink primitive is needed."

7. **The manifest has no `zone: "marks"` entry** (zero marks → zero manifest entries). The `npm run lesson:check` bbox check is trivially clean for marks. The measured pass (per the W4a log) reports 0 measured collisions across 54 peak frames; this run does not change that (no scene code change here).

8. **The "if a future revision reverses the N/A call" clause is explicit in §4.** The new W4b spec is BOUND to the current visual-design's "N/A". If a future W2a decides the lesson DOES want ink (e.g. moves to a different climax mechanic), the W4b spec must be re-run from the new visual-design — never silently re-introduce the old underline. The §4 hand-off is explicit on this.

## ISSUES

(none blocking)

- (informational) The 0-mark spec is a substantive change from the prior W4b run's 1-mark spec. The W4a composer (still on the pre-rename 9-cue namespace, with the `underlineStateAt` helper) is a downstream consumer that needs a re-run. The re-run is independent of this W4b delivery (W4a is parallel, not downstream-of-W4b) and will be picked up by the W4a re-run node when it executes.
- (informational) The v4 cue-id rename (W3.5 re-run at 11:40) is the only W3.5 that has been re-run after the v4 migration. The W4a / W5 / W6 nodes are all still on the pre-rename namespace. The W4b spec is correct for the v4 namespace; the downstream re-runs (W4a, W5, W6) are queued.
- (informational) The visual-design §6 REUSE table does NOT list `<TeacherMark>` (it lists `LessonIntroCard`, `UnitBlock`, `PartWholeComposer`, `RecapSpotlight`, `Breathe`, `motion-vocabulary`, `lesson-caption-layer` as the REUSE entries, and `lesson-bgm-layer` / `lesson-sfx-layer` as the audio wiring entries). `<TeacherMark>` is in the catalog's REUSE list at the W3b scan level (because the catalog says it's registered) but the W2a visual-design author chose not to name a use for it. The "N/A" + the absent §6 entry are two independent upstream signals of "no teacher marks"; the W4b spec honors both.

## PIPELINE FINDINGS

(workflow backlog — what should be IMPROVED about THIS node or the workflow as a whole)

- **P0 — The W4a composer's `underlineStateAt` helper and `<TeacherMark>` import are drift from the visual-design's "N/A" directive.** The prior-run W4a composer (mtime 10:13, pre-W3.5-rename) built a state helper and import for a `cue-split-3of3` underline that the visual-design §3 explicitly forbids. The W3.5 re-run (11:40) renamed the cue ids but did not trigger a W4a re-run, so the drift persists in the W4a source. The W4b spec is the upstream call to drop the helper; the W4a re-run is the mechanical step. **Owns-by:** the W4a re-run node (will pick up the new W4b spec). **Workflow improvement:** the W3.5 cue-id-rename event should be the trigger for a W4a re-run, not a separate signal. A node that re-issues a v4 timeline should mark downstream W4a / W5 / W6 as `stale` so the driver re-runs them; the current W3.5-re-run log doesn't do that.

- **P1 — The pre-rename cue-id namespace (9 cues with `cue-` prefix) and the v4 cue-id namespace (6 cues without prefix) are hard to disambiguate in shared artifacts.** The prior W4b spec's table uses `cue-announce-split-1of5` / `cue-conserve-1of5` / `cue-split-3of3` / etc.; the new W4b spec uses `routine-reprise` / `split-1-and-5` / `split-3-and-3` / etc.; the W3b gap-scan log uses a mix (the gap-scan refers to the 9-cue storyboard structure for its teaching-action tags, but the catalog primitive names are cue-id-agnostic). A future grep on "cue-split-3of3" will find a stale reference in the prior W4b / W4a / W5 logs. **Recommendation:** the v4 rename should retire the old ids (a W1 re-plan that re-bundles the 9 storyboard cues into 6 v4 cues and assigns the new ids at the W1 level); the W3.5 reconcile log should declare the rename explicitly and forbid downstream artifacts from using the old ids; a `grep` lint on the v4 cue-id set in downstream artifacts (W4a / W4b / W5) would catch any stale reference. **Owns-by:** the W1 storyboard skill (re-bundle) + the W3.5 reconcile skill (forbid old ids) + a v4-cue-id lint.

- **P2 — The 0-mark spec is sensitive to upstream re-revisions.** If a future W2a decides the lesson DOES want ink (e.g. a different climax mechanic), the W4b spec must be re-run from the new visual-design. The current spec's §4 hand-off says this explicitly. **Recommendation:** the W4b SKILL should have a "no-mark" exit path (like the W3b "no-gap" exit path) — a sub-200-line spec that says "0 marks, see visual-design §N" without re-deriving the per-cue restraint argument. The current spec is ~8 KB and spends most of its bytes on the per-cue table's 1-line reasons; a future "0 marks" lesson could ship a 2-KB spec. **Owns-by:** the W4b skill (add the no-mark exit path).

- **P3 — The `cue-` prefix is a useful disambiguator that the v4 rename dropped.** The pre-rename 9-cue namespace used `cue-` prefix on every cue id (`cue-announce-split-1of5`, `cue-split-3of3`, etc.); the v4 6-cue namespace drops the prefix (`split-3-and-3`, `routine-reprise`). The prefix made it trivial to distinguish "this is a cue id" from "this is a primitive name" or "this is a property name" in shared artifacts. The v4 namespace has occasional collisions: `split-3-and-3` and `split-1-and-5` look like method names; `recap` is a common noun. **Recommendation:** the v4 namespace should re-introduce a disambiguator (a `c-` prefix, or an `kp6-` lesson-scope prefix, or a `cueId` property wrapper). **Owns-by:** the W1 storyboard skill (cue-id naming convention).

- **P4 — The W3b gap-scan's REUSE list includes `<TeacherMark>` even when the visual-design says "N/A" for sketch tone.** The W3b scan's job is to map demands to registered primitives; "TeacherMark is registered" is a true statement, and the scan includes it in the REUSE list. The visual-design's "N/A" then has to be re-asserted at the W4b level (this run). This is a W3b-vs-W2a coherence gap: the W3b scan should check "is this primitive actually used in the visual-design's reuse table" before including it in the REUSE list. **Recommendation:** the W3b scan should diff its REUSE list against the visual-design §6 reuse table; any primitive in the W3b REUSE list but NOT in the visual-design §6 is a "drift-from-design" primitive and should be flagged (not silently included). **Owns-by:** the W3b skill (diff against visual-design §6).

- **P5 — The W4a composer's `underlineStateAt` helper and the prior W4b's 1-mark spec are a load-bearing example of "W4b says X, W4a builds X, drift hides in both" coupling.** The W4b spec is supposed to be the spec, the W4a scene is supposed to be the realization. If the W4b says "1 mark on split-3of3" and the W4a builds the `underlineStateAt` helper for it, both are individually consistent — but both are inconsistent with the visual-design's "N/A". The W4b spec and the W4a scene are coupled by the spec but both can drift from the upstream visual-design independently. **Recommendation:** the W4a composer should ALWAYS cross-reference the visual-design §3 "Sketch tone" line at the top of its KEY DECISIONS, and the W4b spec should be the first source of truth on the mark count (the W4a scene defers to the W4b spec's mark count). A W4a lint that fails if `<TeacherMark>` is mounted but the W4b spec says 0 marks would catch the drift. **Owns-by:** the W4a skill (cross-ref visual-design §3) + a W4a lint.

+exit 0
