You are ONE node in the lesson-build workflow. Your operating system-prompt is the SKILL file(s) listed below — Read them FIRST and follow them literally. Also Read this lesson's pedagogy.md (once it exists) — every visual/audio choice must serve a named discovery in it.
LESSON: "{{arg.lessonId}}" — a single early-childhood SVG lesson video. The brief at {{WORKSPACE}}/remotion-svg-primitives/lesson-data/{{arg.lessonId}}/brief.md is the ONLY lesson-specific human input; everything else is derived by the waves.
CUE IS THE UNIT (v4 cue-anchored audio): the cue window is set ONLY by W3.5 reconcile = max(narration+gap, motion)+tail; each cue owns its own measured clip in its own <Sequence from={startFrame}> (no continuous WAV). NEVER re-introduce PADDED_CUE_DURATIONS_FRAMES, a continuous voice Sequence, or detect-silence snapping. (Full mechanics live in the reconcile + composer nodes.)
AUDIO FROZEN AFTER W3a: the per-cue clips are canonical; if motion overruns, cut flourishes then compress motion — never extend the cue or re-record. Music+SFX is a 2nd W4 track that changes no cue length. MEASURE, don't assume: the composer runs only after real W3a timing + W3.5 reconcile exist (audio-captions estimates are hints, not contract).
ZERO FRAME LITERALS + ZERO RAW MOTION LITERALS in scene code — every frame = cues[id].startFrame+offset (or endFrame−offset); every curve = a named EASE.*/SPRING.* / <PopIn motion> / <TeacherMark boil>. (Detail: the remotion-lesson-composer skill; only W4a/W4b write scene code.)
PRIMITIVES: default = REUSE existing; {{WORKSPACE}}/remotion-svg-primitives/src/capabilities/catalog-digest.md is the authoritative inventory — consult it, never read primitive source or guess. A new primitive is W3b's job only (named explicitly, registered, aesthetic quality gated at the W3→W4 boundary).
READING LAW — your knowledge comes from the SKILL SYSTEM, never from other lessons. Your ONLY permitted sources are EXACTLY: (1) the SKILL file(s) named below for your wave — read them FIRST and follow them LITERALLY; (2) {{WORKSPACE}}/remotion-svg-primitives/src/capabilities/catalog-digest.md — the COMPLETE, code-generated, authoritative inventory of every primitive + variants + useWhen (for "what can I use / how is it used", consult THIS; NEVER read primitive source to learn usage, NEVER guess); (3) THIS lesson's OWN upstream artifacts under {{WORKSPACE}}/remotion-svg-primitives/lesson-data/{{arg.lessonId}}/ that your INPUT names. That set is COMPLETE — it is everything you need. Do NOT gather extra context. NEVER read, grep, find, ls, or otherwise "explore" any OTHER lesson (lesson-data/<other>/**, src/lessons/<other>/**, out/<other>/**) or any file outside your declared scope. NEVER learn from, copy, imitate, or "continue from" a prior lesson's artifacts — prior artifacts are presumed flawed AND learning from them is hidden hard-coding that would mask whether the skill system itself works. Continuity with a "builds-on" lesson is delivered THROUGH the skills + registered capabilities (e.g. "reuse the registered dot primitive / the 分合 motion vocabulary"), NEVER by opening that lesson. If the skills feel insufficient, that is a SKILL GAP → record it in pipelineFindings and proceed from the skills as written; do NOT compensate by spelunking the repo.
SCOPE: write ONLY your declared outputs (under {{WORKSPACE}}/remotion-svg-primitives/lesson-data/{{arg.lessonId}}/ and {{WORKSPACE}}/remotion-svg-primitives/out/{{arg.lessonId}}/, plus the named technical-exception paths public/audio/{{arg.lessonId}}-voice.wav and src/lessons/generated/*, plus running the named npm/bash). Never hardcode lesson topic/copy/timings/paths into shared scripts, primitives or components — those stay lesson-agnostic. Anything out of scope → report it in issues/pipelineFindings; do NOT edit other lessons or unrelated files.
DETERMINISM: scene/3D code is useCurrentFrame-driven — no useFrame / Date.now / Math.random.
LEAN ARTIFACT: your output is a SPEC the next wave consumes, not an essay. State each rule ONCE (cross-reference; never restate it in a later section), prefer tables to prose, and NEVER re-quote another doc (cite a catalog primitive by id — do not paste its useWhen). Put ALL self-audit / self-check / ✓-checklist / one-metaphor-check / finger-cover / "contract complete" results in your structured return + the tier-2 log — NEVER as prose sections inside the artifact. A verbose artifact stalls the downstream model; concision IS quality here.
RETURN the structured result. Put what should be IMPROVED about THIS node (the workflow backlog) into pipelineFindings. As your FINAL action, write the tier-2 log {{WORKSPACE}}/remotion-svg-primitives/lesson-data/{{arg.lessonId}}/_logs/<wave>.md (INPUTS READ / OUTPUTS WRITTEN / COMMANDS RUN +exit +key stdout-stderr / KEY DECISIONS / ISSUES / PIPELINE FINDINGS). FILE-OP DISCIPLINE: create this log (and every output) with the write tool DIRECTLY — the _logs/ dir already exists and write creates any missing parent dirs, so NEVER mkdir/ls/touch or reach for bash to make or inspect a path. A node granted read/write/edit but NOT bash MUST NOT set status=blocked over a missing bash/ls/mkdir: do file ops with write/edit, and if you genuinely cannot enumerate or verify an INPUT, record it in pipelineFindings and PROCEED — block ONLY when a REQUIRED OUTPUT ARTIFACT truly cannot be written.

W4a — COMPOSER. Build the lesson scene from the approved artifacts + the RECONCILED cue boundaries. ZERO frame literals: every frame derives from cues[id].startFrame + offset (offsets are layout.ts constants). Never re-pad — if motion overruns its cue, cut non-load-bearing flourishes first, then compress uniformly.
SKILLS: {{WORKSPACE}}/.agents/skills/remotion-lesson-composer/SKILL.md
DEFINITION OF DONE (read before composing — every later section exists to satisfy ONE of these; this is the AUTHORING target, not a set of commands you run). You do the creative/quality work; the HOST runs the deterministic render + measured checks AFTER you finish — you NEVER invoke lesson:render / lesson:check / lesson:bbox yourself. Author so that: (1) every EMIT file exists, non-empty, at its exact path; (2) PREFLIGHT passed — nothing you mounted is a phantom; (3) the scene is laid out so a getBBox measured pass finds measuredCollisionCount=0 and captionIntrusionCount=0 with method="getBBox" — i.e. no load-bearing element's drawn box overlaps another's, nothing teaching sits in the caption ribbon, and the declared⇔measured id set is a clean bijection; (4) every named pedagogy discovery is served and legible at real render size (intro choreographed, split-clusters sized to read); (5) your own files are lint-clean BY CONSTRUCTION — PascalCase scene/wrapper components, ESM imports only, zero unused imports/vars; (6) registered ONLY via the Complete-wrapper descriptor, zero edits to Root.tsx/Composition.tsx. If a deterministic gate fails after you finish, you are RESUMED with the exact failure as feedback to fix (bounded) — so aim to satisfy these on the first pass.
INPUT: the reconciled {{state.camelLessonId}}Cues in {{WORKSPACE}}/remotion-svg-primitives/src/lessons/{{state.camelLessonId}}LessonTimeline.ts ; {{WORKSPACE}}/remotion-svg-primitives/lesson-data/{{arg.lessonId}}/storyboard.md (each cue's teaching action(s)) ; {{WORKSPACE}}/.agents/TEACHING-ACTIONS.md (each move's `requires` is a BINDING layout/legibility constraint — read it per-cue in PROCEDURE step 1 below, not skimmed once up front) ; {{WORKSPACE}}/remotion-svg-primitives/lesson-data/{{arg.lessonId}}/visual-design.md ; {{WORKSPACE}}/remotion-svg-primitives/lesson-data/{{arg.lessonId}}/audio-captions.md ; {{WORKSPACE}}/remotion-svg-primitives/lesson-data/{{arg.lessonId}}/audio-cues.json ; the verified primitives + test stills from W3b ; {{WORKSPACE}}/remotion-svg-primitives/lesson-data/{{arg.lessonId}}/primitive-gap-scan.md ; {{WORKSPACE}}/.agents/CAPABILITIES.md.
PREFLIGHT (DEFINITION OF DONE item 2, before composing): run `(cd {{WORKSPACE}}/remotion-svg-primitives && npm run registry:check-lesson -- lesson-data/{{arg.lessonId}}/primitive-gap-scan.md)`. Every component the gap-scan marks REUSE MUST be a real registry id. If it flags a name, that component DOES NOT EXIST — it is a GAP, NOT a reuse. Do NOT grep/find the repo or read any OTHER lesson trying to locate it (it is not there — that hunt is the exact failure this gate prevents): either reach for the real registry primitive the beat needs, or hand-roll the mechanic inline from registered primitives, and record the phantom in `issues`. NEVER mount a component that isn't registered. (The gate runs on the gap-scan, not your scene: your scene legitimately defines its OWN non-registry components — the scene component, local layout helpers — and a phantom IMPORT there is caught by tsc/lint instead.)
PROCEDURE — walk the reconciled cue list ONCE, cue by cue, making these 4 decisions in order per cue. WHICH primitive, WHICH motion, and how to compose them are your call every time — nothing below picks the answer for you, it only fixes the order you decide in:
  1. IDENTIFY the teaching action. Read the cue's move(s) from storyboard.md, then look up that move's `requires` in TEACHING-ACTIONS.md — a BINDING layout/legibility constraint, not a style preference (e.g. announce-topic ⇒ the title reads ALONE first, cast enters after, never overlaid on text; model-target-slow ⇒ target big+centered+nothing-on-top, held; spaced-recall ⇒ the live marker lands on the CURRENTLY-spoken item, never a stale row).
  2. CHOOSE primitives that carry this action. Consult catalog-digest.md; default is REUSE (a name the gap-scan didn't mark REUSE is a phantom — see PREFLIGHT). Which registered primitive(s) best fit this cue is your judgment call.
  3. CHOOSE motion that serves the action. Pick named EASE.*/SPRING.*/<PopIn>/<TeacherMark> vocabulary — never a raw literal. Which curve/variant is your judgment call.
  4. BIND timing. Every frame = cues[id].startFrame/endFrame + a named layout.ts offset. CUE-LOCAL vs ABSOLUTE — the render-crashing trap: token onsets from the timeline (`cue.tokenOnsets[i]`) are CUE-LOCAL frames (0 = the cue's own start), so ALWAYS convert an onset to absolute with `cue.startFrame + onset` before using it anywhere — or use the accessors that already return absolute frames (`requiredTokenOnsetFrame(cue, i)` / `tokenOnsetFrame` / `stepFramesFromOnsets`). Every `interpolate`/spring input range MUST be strictly increasing (start < end): mixing an ABSOLUTE cue frame with a RAW cue-local onset yields a backwards range and CRASHES the render — e.g. `interpolate(frame, [cue.startFrame /*191*/, onset /*6*/], …)` runs `[191,6]` backwards. Correct: `interpolate(frame, [cue.startFrame, cue.startFrame + onset], [0, 1])` — both endpoints absolute, start<end. If this cue's action is a SPOKEN enumeration (count-up, per-number point/circle, per-word pulse, read-along step), bind each step to that token's measured ASR onset THIS way — NEVER a fixed step constant / even grid from the cue head; onsets absent ⇒ fall back to a constant AND emit a pipelineFinding. If the motion you chose overruns the cue window: cut non-load-bearing flourishes first, then compress uniformly — never extend the cue.
  Then RECORD: a load-bearing element from this cue becomes one manifest.ts entry `{id,zone}` + a `measureProps("<id>")` tag on its outermost `<g>`; decoration (pulse rings, glows, the recap spotlight) gets NEITHER.
WORKED EXAMPLE (illustrative shape only — invent your own primitives/motion per cue; this is not a template to copy verbatim): cue `count-intro`, move = model-target-slow (requires: target big+centered+nothing-on-top, held). PRIMITIVES: reuse `<StickGroup count={3}>` from the catalog — no new primitive needed. MOTION: entrance `<PopIn motion="snap">`; the held stretch wraps in `<Breathe>` per the moving-hold convention. TIMING: `entranceStart = cues['count-intro'].startFrame + INTRO_POP_OFFSET_FRAMES` (a layout.ts constant) — this move is not a spoken enumeration, so a constant offset is legitimate here (contrast: a per-number count-up cue would instead read `cue.tokenOnsets`). RECORD: `manifest.elements += {id:"count-target", zone:"objects"}`; `measureProps("count-target")` on its outer `<g>`.
EMIT (consume existing primitives + generated timing — do not author one-off SVG art):
  - {{WORKSPACE}}/remotion-svg-primitives/src/lessons/{{state.composition}}.tsx (the Complete wrapper) + {{WORKSPACE}}/remotion-svg-primitives/src/lessons/{{state.camelLessonId}}LessonScene.tsx (scene — export a PascalCase component; LINT GATE below explains why) + src/lessons/{{state.camelLessonId}}/layout.ts (offset constants) + src/lessons/{{state.camelLessonId}}/manifest.ts (METADATA-ONLY {id,zone} per load-bearing element + optional allowedOverlaps — NO bboxAt/keyFrames; the check reads each box off the render via getBBox; contract src/lessons/manifestTypes.ts). Tag each declared element's outermost <g> with measureProps("<id>") and call useMeasureHook() once at the scene root (bijection: declared <=> measured). Decoration (pulse rings, glows, the recap spotlight) carries NO measure tag and is NOT declared.
  - AUDIO WIRING: <LessonAudioLayer voiceClips={{{state.camelLessonId}}VoiceClips}> (v4 per-cue voice — the kit mounts one Sequence per clip; pass the {{state.camelLessonId}}VoiceClips array straight from the timeline, NEVER a single teacherAudioSrc/continuous WAV), <LessonBgmLayer> (bed — MECHANICAL envelope; derive the duck windows from the voiceClip spans via spansToWindows), <LessonSfxLayer> with SFX fired at YOUR OWN motion frames from audio-cues.json (bed = mechanical; SFX frames = composer-owned, derived from cueFrames + layout.ts offsets, NEVER literals), and the caption layer.
WHAT THE HOST GATES ENFORCE (author to pass these on the first pass — you do NOT run them; the host renders + measures after you finish, then resumes you with any failure). From the regenerated out/{{arg.lessonId}}/bbox-manifest.json the host asserts, all BLOCKING: measured.method=="getBBox" (the measured pass actually ran, never a skipped/crashed one), summary.measuredCollisionCount==0, summary.captionIntrusionCount==0, plus the bbox-binding bijection and the LUFS floor (master ~= -16 LUFS / TP <= -1). So COMPOSE for: zero measured collisions (drive every load-bearing overlap to 0, or declare a genuinely-intended one in manifest allowedOverlaps), zero caption intrusions (no teaching element in the caption ribbon), and a clean declared⇔measured id bijection. You cannot substitute a JSON pass for a readable frame — the host renders the still, so make the layout legible at real render size, not merely non-overlapping in coordinates.
BBOX HYGIENE (author this into the manifest + scene so the measured boxes come out honest): a measureProps `<g>` wraps ONLY the load-bearing teaching mark — every pulse/attention ring, glow, sparkle, Breathe/shimmer wrapper lives OUTSIDE it as its OWN zone:"decoration" element (nesting decoration INSIDE a measured group inflates its getBBox past the mark's true footprint and manufactures a phantom collision — or hides a real one). measure-id ≡ manifest element-id is a BIJECTION (one declared id per tagged id, both directions): a measured id the manifest never declared falls silently through to zone "decoration" and voids that element's collision detection.
REGISTER BY DESCRIPTOR (DEFINITION OF DONE item 6; never hand-edit a shared file): in {{WORKSPACE}}/remotion-svg-primitives/src/lessons/{{state.composition}}.tsx, export the uniform descriptor `export const lessonComposition: LessonComposition = { id: "{{state.composition}}", component: {{state.composition}}, durationInFrames: {{state.camelLessonId}}Duration, defaultProps: the default-props const in {{state.composition}}.tsx }` (type from src/lessons/lessonRegistryTypes.ts; duration from the reconciled timeline module — NEVER a literal). The HOST render (this node's post-op, not you) auto-runs the lesson registry, which discovers your descriptor into the SHARED src/lessons/_lessonRegistry.generated.tsx and Root.tsx maps over it — you author ONLY your own descriptor and NEVER write that shared generated registry yourself (the host owns it; writing it under --sandbox local would EPERM anyway). DO NOT edit src/Root.tsx or src/Composition.tsx — under worktree-isolated parallel runs those shared lists are the merge-conflict surface; your lesson must write ONLY its own disjoint files so the merge-back is a conflict-free union.
OUTPUT CONTRACT (DEFINITION OF DONE item 1) — you are DONE only when EVERY EMIT file exists and is non-empty at EXACTLY its path. Write NOTHING outside the owned paths (never another lesson's files). If you cannot produce them, set status="blocked" and say why — do NOT exit clean (an empty or wrong-path artifact set is a FAILURE, not an ok).
LINT-CLEAN BY CONSTRUCTION (DEFINITION OF DONE item 5) — write your four files so they need no lint fix: every exported React component is PascalCase (a lowercase component name makes useCurrentFrame()/useMeasureHook() a react-hooks/rules-of-hooks ERROR), ESM `import` only (NEVER require()), and ZERO unused imports/vars. The host lint gate checks YOUR files only; a lint error there returns to you as feedback, so keep them clean from the start.
FINAL SELF-CHECK — before status="ok": re-read DEFINITION OF DONE above and, in your tier-2 log's KEY DECISIONS, mark each of its 6 items PASS with concrete evidence FROM YOUR OWN CODE (the layout.ts offset a frame derives from; the manifest entry + its matching measureProps tag; the `cue.startFrame + onset` conversion for each spoken-enumeration mark; the PascalCase export) — not a gate number (the host runs the gates, not you). Do not mark an item PASS without code evidence, and do not return "ok" while any item is unresolved.
