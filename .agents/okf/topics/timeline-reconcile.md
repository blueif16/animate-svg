---
type: subsystem
key: timeline-reconcile
title: Timeline reconcile (cue-anchored per-lesson LessonTimeline.ts)
description: How W3.5 reconcile turns the frozen per-cue audio clips + a per-lesson motion budget into the reconciled cue timeline every scene reads — the local cueAccessors resolver + the external reconcileClipTimeline boundary.
resource: remotion-svg-primitives/src/lessons/_cues/cueAccessors.ts
aliases: [reconcile, cue accessors, LessonTimeline, cue window, w3-5-reconcile, narration-kit]
seeds: [remotion-svg-primitives/src/lessons/_cues/cueAccessors.ts, remotion-svg-primitives/src/lessons/kptestCountToTwoLessonTimeline.ts, remotion-svg-primitives/src/lessons/generated/kptestCountToTwoClips.ts]
symbols: [makeCueAccessors, CueAccessors, reconcileClipTimeline, cueToCaption]
tags: [timeline, reconcile, cue, voice-clip, w3-5-reconcile]
timestamp: 2026-07-09
---

# Why / how it works (the lifecycle, end to end)
`w3-5-reconcile`'s ONE owned artifact per lesson is `src/lessons/<CamelId>LessonTimeline.ts` — generated code
that calls `reconcileClipTimeline()` (imported from the sibling package `@studio/narration-kit`, a `file:`
dependency living OUTSIDE this repo at `../../shared-narration` — the reconcile ALGORITHM itself is out of this
repo's product-code boundary) over the frozen per-cue clips (`src/lessons/generated/<CamelId>Clips.ts`, W3a's
audio truth) plus a per-lesson `VISUAL_MOTION_SECONDS` budget map sourced from `visual-design.md`. The window
formula (`max(narration+gap, motion)+tail`) lives inside the vendored kit; what's LOCAL and load-bearing is the
cross-check that throws when the budget map's cue-id keys diverge from the frozen clip ids, and
`makeCueAccessors` (the ONE place a lesson resolves a cue id → frame), which replaces a silent `?? 0` fallback
with a hard throw naming the bad id + the valid set. The composer (`w4a`) and sketch overlay (`w4b`) both read
the reconciled `<CamelId>Cues` this node writes — never re-deriving frames themselves.

# Anchors
RECONCILE (per-lesson generated output)
- `remotion-svg-primitives/src/lessons/kptestCountToTwoLessonTimeline.ts:61` — `reconcileClipTimeline({...})` call-site — chains frozen clips + motion budget into the reconciled cue timeline
- `remotion-svg-primitives/src/lessons/kptestCountToTwoLessonTimeline.ts:20` — `import { reconcileClipTimeline } from "@studio/narration-kit"` — the algorithm lives in the vendored sibling package, NOT this repo
ACCESS (local, owned)
- `remotion-svg-primitives/src/lessons/_cues/cueAccessors.ts:33` — `makeCueAccessors()` — cue-id → frame resolver; THROWS on an unknown id instead of silently defaulting to frame 0
- `remotion-svg-primitives/src/lessons/_cues/cueAccessors.ts:22` — `CueAccessors<Id>` type — the accessor contract every scene consumes
FROZEN INPUT
- `remotion-svg-primitives/src/lessons/generated/kptestCountToTwoClips.ts:10` — `kptestCountToTwoClips` — the measured per-cue audio truth (W3a's output) reconcile chains

# Freshness (anti-drift)
anchors ✓ (every line opened + confirmed) · scope = the seeds above · re-derive when they change · DRIFT NOTE:
`reconcileClipTimeline`/`cueToCaption` have NO local definition — they are an EXTERNAL BOUNDARY (the
`@studio/narration-kit` package, symlinked via `file:../../shared-narration` in package.json, physically at
`/Users/tk/Desktop/shared-narration`, a sibling repo this OKF slice does NOT cover). Treat that package as a
vendored dependency: anchor on the CALL SITE + the frozen-clip cross-check here; escalate to the sibling repo
(out of scope) only if the reconcile algorithm itself is in question. Every lesson repeats this pattern in its
own `<CamelId>LessonTimeline.ts` (kptestFirstSecondThird, kptestCompareMoreFewer, kptestFenyuheSix,
kp2CountingByTens, kptestGreetingsVerify) — kptestCountToTwo is cited as the representative instance.

<!-- okf:auto-start -->
> _Auto-generated by `_generate.mjs` — do not hand-edit between the markers; re-run `--write`._

### Final state — file set (seeds)

| File | exists |
|---|---|
| `remotion-svg-primitives/src/lessons/_cues/cueAccessors.ts` | ✓ |
| `remotion-svg-primitives/src/lessons/kptestCountToTwoLessonTimeline.ts` | ✓ |
| `remotion-svg-primitives/src/lessons/generated/kptestCountToTwoClips.ts` | ✓ |

### Evolution arc

- `3bfba64` 2026-07-01 — test(lesson): add kptest-count-to-two fixture from piflow template run
- `4a26040` 2026-07-03 — feat(lessons): throwing cue-id accessor factory (kills silent ?? 0)
- `c25c4b6` 2026-07-03 — fix(lessons): adopt throwing cue accessors; re-key count-to-two to its 3-cue audio truth

### Lessons — memory cluster

_memory dir not found — lessons skipped_
### Code anchors / blast radius (codegraph)

- `makeCueAccessors` (remotion-svg-primitives/src/lessons/_cues/cueAccessors.ts:33) — 9 callers in `remotion-svg-primitives/src/lessons/kptestCountToTwoLessonScene.tsx`, `remotion-svg-primitives/src/lessons/kptestCountToTwoLessonTimeline.ts`, `remotion-svg-primitives/src/lessons/kptestFirstSecondThirdLessonScene.tsx`, `remotion-svg-primitives/src/lessons/CompleteKp2CountingByTensLesson.tsx` +2 more; tests: `remotion-svg-primitives/src/lessons/_cues/cueAccessors.test.ts`
- `CueAccessors` (remotion-svg-primitives/src/lessons/_cues/cueAccessors.ts:22) — 1 caller in `remotion-svg-primitives/src/lessons/_cues/cueAccessors.ts`; ⚠ no covering tests found
- `Cue` (remotion-svg-primitives/scripts/_padded-cues-extract.ts:42) — 2 callers in `remotion-svg-primitives/scripts/_padded-cues-extract.ts`, `remotion-svg-primitives/src/lesson-media/captionKeywords.ts`; ⚠ no covering tests found
- `caption` (remotion-svg-primitives/src/scenes/OrdinalLabelTokenDemo.tsx:32) — 1 caller in `remotion-svg-primitives/src/scenes/OrdinalLabelTokenDemo.tsx`; ⚠ no covering tests found

<sub>derived 2026-07-09 · arc=3 commits · files=3 · lessons=0</sub>
<!-- okf:auto-end -->
